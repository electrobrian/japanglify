*.iml
.gradle/
/.gradle-user-home/
/.android-user-home/
/local.properties
/.idea/
.DS_Store
/build/
**/build/

# Device UAT dumps (Discord chat, notifications, DUT screens, pulled
# Copy-image PNGs). Covered by **/build/ when they live under
# app/build/reports/acceptance/ — listed explicitly so they never get
# committed if someone copies them out of build/.
app/build/reports/acceptance/
**/acceptance/beta1/
/captures/
.externalNativeBuild/
.cxx/
*.apk
*.ap_
*.aab
*.dex
*.class
bin/
gen/
out/
.kotlin/
sdk/
.android-sdk/
*.log
/scripts/logs/

# Release signing — local to each machine, never committed.
/keystore.properties
*.keystore.jks
*.jks
*.keystore

# Editor backup files
*~

# Claude Code session transcript dumps
*-this-session-is-being-continued-from-a-previous-c.txt
